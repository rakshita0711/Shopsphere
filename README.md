# Shopsphere
ShopSphere is a full-stack POS / e-commerce web application built using React, Node.js, Express, and MongoDB. The project includes Admin and User dashboards, complete CRUD operations for master tables like Categories and Products, image upload with preview, and role-based access control.
